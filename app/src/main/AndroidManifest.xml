<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.twhaKtApp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".TwhaIntentService"
            android:exported="false" />

        <activity android:name=".view.ui.main.TwhaStatsActivity" />
        <activity android:name=".TwhaQuestionActivity" />
        <activity android:name=".MainTwhaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- ブロードキャストレシーバーと受け取るアクション名「MY_INTENT」を登録 -->
        <receiver android:name=".mBroadcastReceiver">
            <intent-filter>
                <action android:name="TWHA_INTENT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>